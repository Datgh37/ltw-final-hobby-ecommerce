@model IReadOnlyList<TuNhanTamTInh_Ecommerce.Models.ViewModels.ProductCardViewModel>

<div class="row">
    @if (Model.Count == 0)
    {
        <div class="col-12">
            <div class="alert alert-info mb-0">No products found.</div>
        </div>
    }
    else
    {
        foreach (var item in Model)
        {
            <partial name="_ProductCard" model="item" />
        }
    }
</div>
